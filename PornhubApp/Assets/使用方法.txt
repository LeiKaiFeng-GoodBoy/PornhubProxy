1.(可选)Assets文件夹中有一个myCA.pfx的证书，把它导入根证书存储区，假如网站忽略隐私错误可以正常访问，我建议你不要导入
2.运行win-x64文件夹中的PornhubApp.exe
3.浏览器访问https://cn.pornhub.com/ (假如第一步没有做，那么浏览器会告诉你隐私错误,你要做的就是忽略隐私错误)

其他也暂时支持的网站
https://www.apkmirror.com/
https://www.uptodown.com/android



瞎扯一下原理，能不能不安装自己的证书却能绕过SNI阻断？
假如对方的默认的证书备用名称里存在目标的名字，单纯劫持浏览器的Client Hello然后修改SNI的值，比如发一个空值，或者说发一个不会触发SNI并且证书备用名称中存在的值，这样服务器端返回的证书浏览器应该也会验证通过
但是我没有试过
因为需要
1.需要自己写一个解析Client Hello的程序，或者有库
2.TLS协议必须不校验Client Hello有没有被修改，我不太清楚它验不验，按道理将单纯改一个SNI，服务器返回的证书没有问题，浏览器应该也没有问题